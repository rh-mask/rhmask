# dependencies
node_modules/

# next.js
.next/
out/
next-env.d.ts
*.tsbuildinfo

# env
.env
.env.*
!.env.example

# vercel
.vercel/

# os / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# internal research and working notes - never published
internal/

# extension bundle
extension/dist/
cli/dist/

# campaign binaries: rendered artefacts, not source. They are regenerated from
# the HTML in the session scratchpad, so git is the wrong place to keep them.
marketing/posters/*.jpg
marketing/posters/*.png
marketing/video/*.mp4

# solc artefacts, rebuilt with npm run contracts:build
contracts/out/

# Assistant rule files some tools drop into the project root.
# They are tooling, not source, and the contributor list stays human.
AGENTS.md
CLAUDE.md
.cursorrules
.cursor/
.aider*
.github/copilot-instructions.md
